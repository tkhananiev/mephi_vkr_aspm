rules:
  - id: demo-shell-injection
    languages:
      - python
    severity: ERROR
    message: User input reaches subprocess with shell=True.
    patterns:
      - pattern: subprocess.run(..., shell=True, ...)
    metadata:
      cwe:
        - CWE-78
